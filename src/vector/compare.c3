module radiace::vector;

// fn Vmask cmp_eq(Vector x, Vector y)
// {
//
// }
//
//
// fn Vmask cmp_ne(Vector x, Vector y)
// fn Vmask cmp_lt(Vector x, Vector y)
// fn Vmask cmp_le(Vector x, Vector y)
// fn Vmask cmp_gt(Vector x, Vector y)
// fn Vmask cmp_ge(Vector x, Vector y)

